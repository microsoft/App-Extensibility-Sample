<Page
    x:Class="ExtensibilitySample.ExtensionsTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExtensibilitySample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ItemTemplate" x:DataType="local:Extension">
            <RelativePanel Height="80" HorizontalAlignment="Stretch" Width="323">
                <TextBlock x:Name="ExtensionTitle" Text="{x:Bind AppExtension.DisplayName}" HorizontalAlignment="Stretch" VerticalAlignment="Center" 
                           RelativePanel.AlignLeftWithPanel="True"/>
                <TextBlock Text="{x:Bind AppExtension.Description}" HorizontalAlignment="Stretch" TextWrapping="Wrap"
                           RelativePanel.AlignLeftWithPanel="True"
                           RelativePanel.Below="ExtensionTitle"/>
                <CheckBox IsChecked="{Binding Path=Enabled}" VerticalAlignment="Center" 
                          RelativePanel.AlignRightWithPanel="True" 
                          MinWidth="30" Checked="CheckBox_Checked"
                          Unchecked="CheckBox_Unchecked"
                          />
            </RelativePanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="MobileLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SearchBox.Visibility" Value="Collapsed" />
                        <Setter Target="CompactSearchBox.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="850" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SearchBox.Width" Value="200" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Pivot>

            <Pivot.LeftHeader>
                <AppBarButton Icon="Back" Click="BackButton_Click" />
            </Pivot.LeftHeader>

            <Pivot.RightHeader >
                <Grid>
                    <AppBarButton Icon="Find" x:Name="CompactSearchBox" Visibility="Collapsed"
                                  VerticalAlignment="Stretch">
                        <AppBarButton.Flyout>
                            <Flyout>
                                <AutoSuggestBox  
                                VerticalAlignment="Center" Width="250" Margin="4,4,8,4" 
                                QueryIcon="Find" PlaceholderText="enter a search term"
                                TextChanged="AutoSuggestBox_TextChanged"
                                QuerySubmitted="AutoSuggestBox_QuerySubmitted"
                                SuggestionChosen="AutoSuggestBox_SuggestionChosen"
                                ItemsSource="{x:Bind Suggestions}"/>
                            </Flyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>

                    <AutoSuggestBox x:Name="SearchBox"  
                                VerticalAlignment="Center" Width="250" Margin="4,4,8,4" 
                                QueryIcon="Find" PlaceholderText="enter a search term"
                                TextChanged="AutoSuggestBox_TextChanged"
                                QuerySubmitted="AutoSuggestBox_QuerySubmitted"
                                SuggestionChosen="AutoSuggestBox_SuggestionChosen"
                                ItemsSource="{x:Bind Suggestions}"/>
                </Grid>
            </Pivot.RightHeader>

            <PivotItem Header="All">
                <ListView ItemsSource="{x:Bind Items}" ItemTemplate="{StaticResource ItemTemplate}" 
                          SelectionChanged="ListView_SelectionChanged" />
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

